name: medical_app
description: A medical application for managing prescriptions and medications
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  http: ^1.1.0
  shared_preferences: ^2.2.0
  intl: ^0.18.1
  flutter_local_notifications: ^15.1.0+1
  timezone: ^0.9.2
  image_picker: ^1.0.4
  path_provider: ^2.1.1
  sqflite: ^2.3.0
  share_plus: ^7.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
